{
  "name": "@rue-js/ssr-binary-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ssr": "vite build --config vite.config.ts --mode ssr",
    "build:client": "vite build --config vite.config.ts --mode client",
    "build:binary": "cargo build --release --bin rue-ssr-binary-demo",
    "build:desktop": "pnpm run build:ssr && pnpm run build:client && cargo build --release --features desktop-webview --bin rue-ssr-binary-demo-desktop",
    "build": "pnpm run build:ssr && pnpm run build:client && pnpm run build:binary",
    "desktop": "pnpm run build:ssr && pnpm run build:client && cargo run --features desktop-webview --bin rue-ssr-binary-demo-desktop --",
    "start": "pnpm run build:ssr && pnpm run build:client && cargo run --bin rue-ssr-binary-demo --",
    "run": "pnpm run start",
    "render": "pnpm run build:ssr && cargo run --bin rue-ssr-binary-demo -- --render-only"
  },
  "dependencies": {
    "@rue-js/rue": "workspace:*",
    "@rue-js/server-renderer": "workspace:*"
  },
  "devDependencies": {
    "@rue-js/vite-plugin-rue": "workspace:*",
    "@swc/core": "^1.15.33",
    "vite": "catalog:"
  },
  "engines": {
    "node": ">=22.22.0"
  }
}
