// The unified runtime is built by the normal workspace build; no separate
// runtime artifact preparation is required before starting the app.
// Reserved pre-build hook; intentionally kept as a no-op.
export {}
