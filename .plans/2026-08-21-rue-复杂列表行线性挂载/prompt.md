# Rue 复杂列表行线性挂载任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 建立复杂行基线矩阵【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/1-建立复杂行基线矩阵.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 2

## 任务 2: 建立 Solid 式列表行 owner【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/2-建立Solid式行owner.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 3: 索引保守路径映射【批次 2】依赖批次 1，与任务 2 可并行

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/3-索引保守路径映射.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 3

## 任务 4: 拆分直接挂载与 effect 合并【批次 3】依赖批次 2 的任务 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/4-拆分直接挂载与effect合并.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 4

## 任务 5: 建立 ref 行所有权与清理【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/5-建立ref行所有权与清理.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 5

## 任务 6: 拥有条件与嵌套结构子树【批次 5】依赖批次 2、3、4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/6-拥有条件与嵌套结构子树.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 6

## 任务 7: 拥有组件与不透明 renderable【批次 6】依赖批次 4、5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/7-拥有组件与不透明renderable.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 7

## 任务 8: 固化五类线性性能门槛【批次 7】依赖批次 1、2、3、4、5、6

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/8-固化五类线性性能门槛.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```
