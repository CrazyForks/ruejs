# Rue 复杂列表行线性挂载任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。虽然部分任务的技术依赖允许提前开始，但这些任务共享 helper、测试夹具和 release 构建产物，必须按批次 1 至 11 串行执行。

# 批次 1

## 任务 1: 建立复杂行基线矩阵【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/1-建立复杂行基线矩阵.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 2

## 任务 2: 建立可回收的 detached 列表作用域【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/2-建立可回收detached列表作用域.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 3

## 任务 3: 建立 Solid 式列表行 owner【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/3-建立Solid式行owner.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 4

## 任务 4: 索引 anchor/range 身份映射与压缩调度【批次 4】依赖批次 1，按计划仍在批次 3 后执行

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/4-索引身份映射与压缩调度.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 5

## 任务 5: 拆分列表行挂载能力【批次 5】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/5-拆分列表行挂载能力.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 6

## 任务 6: 建立 ref 行所有权与清理【批次 6】依赖批次 3、5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/6-建立ref行所有权与清理.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 7

## 任务 7: 建立传递式 owned mount 协议【批次 7】依赖批次 3、4、6

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/7-建立传递式owned-mount协议.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 8

## 任务 8: 延迟组件 mounted 并回收实例【批次 8】依赖批次 7

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/8-延迟组件mounted并回收实例.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 9

## 任务 9: 拥有同步不透明 renderable【批次 9】依赖批次 8

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/9-拥有同步不透明renderable.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 10

## 任务 10: 处理异步与外部 renderable 边界【批次 10】依赖批次 9

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/10-处理异步与外部renderable边界.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 11

## 任务 11: 固化线性性能与零残留门槛【批次 11】依赖批次 1 至 10

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-21-rue-复杂列表行线性挂载/11-固化线性性能与零残留门槛.md`。

开始前先阅读 `.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-21-rue-复杂列表行线性挂载/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```
