# Runtime Vapor JS 边界重构任务提示词

按顺序复制以下提示到独立对话框执行。所有任务共享入口、构建产物和差分夹具，必须按批次 1 至 17 串行执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 冻结当前三产物边界与基线【批次 1，无依赖】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/1-冻结当前三产物边界与基线.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 2

## 任务 2: 提取共享 reactive 门面【批次 2，依赖批次 1】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/2-提取共享reactive门面.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 1 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 3

## 任务 3: 迁移 Hook 上下文与槽位【批次 3，依赖批次 2】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/3-迁移Hook上下文与槽位.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 2 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 4

## 任务 4: 迁移值工具与响应式包装【批次 4，依赖批次 3】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/4-迁移值工具与响应式包装.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 3 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 5

## 任务 5: 迁移状态、缓存与回调 Hook【批次 5，依赖批次 4】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/5-迁移状态缓存与回调Hook.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 4 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 6

## 任务 6: 迁移 useEffect 调度与清理【批次 6，依赖批次 5】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/6-迁移useEffect调度与清理.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 5 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 7

## 任务 7: 迁移 computed Hook 并收窄 Rust 原语【批次 7，依赖批次 6】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/7-迁移computedHook并收窄Rust原语.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 6 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 8

## 任务 8: 建立 JS Runtime 骨架与输入协议【批次 8，依赖批次 7】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/8-建立JSRuntime骨架与输入协议.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 7 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 9

## 任务 9: 迁移 Host 容器与基础挂载【批次 9，依赖批次 8】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/9-迁移Host容器与基础挂载.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 8 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 10

## 任务 10: 迁移锚点、范围与静态渲染【批次 10，依赖批次 9】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/10-迁移锚点范围与静态渲染.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 9 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 11

## 任务 11: 迁移 Owned Mount 与代际管理【批次 11，依赖批次 10】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/11-迁移OwnedMount与代际管理.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 10 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 12

## 任务 12: 迁移组件实例、Patch 与 Hydration【批次 12，依赖批次 11】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/12-迁移组件实例Patch与Hydration.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 11 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 13

## 任务 13: 迁移生命周期、错误与 KeepAlive【批次 13，依赖批次 12】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/13-迁移生命周期错误与KeepAlive.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 12 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 14

## 任务 14: 迁移应用、插件、SSR 与控制面【批次 14，依赖批次 13】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/14-迁移应用插件SSR与控制面.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 13 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 15

## 任务 15: 分阶段切换 Vapor、完整与 Node 入口【批次 15，依赖批次 14】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/15-分阶段切换Vapor完整与Node入口.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 14 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 16

## 任务 16: 删除 Rust 外壳并统一浏览器内核【批次 16，依赖批次 15】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/16-删除Rust外壳并统一浏览器内核.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 15 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 17

## 任务 17: 全量回归、性能护栏与预算交接【批次 17，依赖批次 16】

```text
请使用技能 /Users/xiangmin/.codex/skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-25-runtime-vapor-js-边界重构/17-全量回归性能护栏与预算交接.md`。

开始前先阅读 `.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 和该任务文件，确认批次 16 已完成。按任务文件中的验证计划实施，只处理回归、测量与交接，不修改性能预算，并保护当前未提交的性能架构改动。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，将状态改为 `已完成，待回归`。
注意，`.plans/2026-08-25-runtime-vapor-js-边界重构/plan.md` 为只读，不要修改该文件的任何部分。边界计划完成后，再执行 `.plans/2026-08-25-性能架构优化/10-固化全链路性能预算.md`。
```
