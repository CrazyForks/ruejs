# 任务 17: 全量回归、性能护栏与预算交接

批次：【批次 17】 依赖批次 16

状态：已阻塞

目的：证明边界迁移没有破坏近期性能架构成果，并把新产物事实交给性能计划任务 10 收口预算。

来源任务：性能架构优化任务 10 的前置依赖

预计会话范围：只执行全量验证、同机对比、差异审查和交接记录；不修改性能 runner、baseline、budget、release 接线或性能文档。

## 文件

- 验证：`packages/runtime-vapor/`
- 验证：`packages/runtime/`
- 验证：`packages/swc-plugin-rue/`
- 验证：`scripts/__tests__/runtime-vapor-artifacts.spec.ts`
- 证据：`temp/runtime-vapor-js-boundary/post-boundary-current.json`
- 交接：`.plans/2026-08-25-性能架构优化/10-固化全链路性能预算.md`

## 上下文

- 性能计划批次 1 至 9 的 compiler、DOM、列表、size 和 benchmark 改动是受保护成果；本任务只验证，不改它们。
- 与任务 1 的“当前大改后基线”比较，核心场景不得出现超过 5% 的可复现退化；Hook-heavy 目标为至少 10% 改善，综合加权目标为至少 5% 改善。最终 0.8.13 相对预算仍由性能任务 10 判定。

## 验证计划

- 行为：完整构建、类型、Rust reactive、unit、列表快路径、产物结构和同机 benchmark 全部通过；任何退化可定位到本计划任务。
- 验证命令：`pnpm run check && cargo test --manifest-path packages/runtime-vapor/Cargo.toml && cargo test --manifest-path packages/swc-plugin-rue/Cargo.toml --test js_framework_benchmark && pnpm run test-unit && pnpm exec vitest run --project unit scripts/__tests__/runtime-vapor-artifacts.spec.ts`
- 性能命令：`pnpm run benchmark:js-framework -- --output temp/runtime-vapor-js-boundary/post-boundary-current.json`
- 预期失败原因：不适用；本任务不改变行为，若验证失败必须回到引入失败的编号任务修复，不能在本任务扩大生产修改。
- 模拟策略：全部使用真实 workspace build、Rust tests、Vitest 与 benchmark；不模拟性能或产物。

## 步骤

1. 审查 git diff，确认没有覆盖近期 compiler、DOM、列表与性能基础设施成果。
2. 依次运行类型、Rust reactive/compiler、全量 unit 和真实 artifact 契约。
3. 在与任务 1 相同环境运行 benchmark，依据两个 JSON 的同名场景中位数制作 CPU、heap、size、first-paint 与 Hook-heavy 对比表；不要借用面向 0.8.13 的最终 budget 判定当前基线。
4. 若边界重构引入失败或超过 5% 的可复现退化，回到来源任务修复并重跑；不修改预算掩盖问题。
5. 验证通过后，将新 canonical `pkg-vapor` 产物结构和测量 JSON 作为性能计划任务 10 的输入，不修改或删除其任务文件。

## 验证

- 运行：上述验证命令与性能命令。
- 预期：命令全部为 0；单浏览器 Wasm 契约成立；当前性能基线无 >5% 退化；改善目标有实测数据，无数据时不得宣称收益。
- 所需证据：所有退出码/失败数、post JSON、pre/post 对比表、Wasm 导出/hash、受保护文件 diff 和性能任务 10 交接清单。

## 完成

本任务是回归与交接，不适用失败验证-实现循环。只有边界计划全部证据完备、性能退化已排除且性能计划任务 10 可直接消费新产物事实时才算完成。

## 阻塞记录（2026-08-26）

- 批次 16 任务文件已按完成流程删除，当前工作树包含其 Rust runtime/hook/stub 删除、单浏览器 `pkg-vapor` 入口和 JS 差分测试；任务 1 的 `temp/runtime-vapor-js-boundary/pre-boundary-current.json` 存在。
- 已运行 `pnpm run check`：退出码 0。
- 已运行 `cargo test --manifest-path packages/runtime-vapor/Cargo.toml`：退出码 0；11 passed，0 failed，1 ignored，其余目标 0 tests。
- 已运行 `cargo test --manifest-path packages/swc-plugin-rue/Cargo.toml --test js_framework_benchmark`：退出码 0；4 passed，0 failed。
- 已运行 `pnpm run test-unit`：退出码 1；296 个测试文件中 59 failed / 237 passed，1100 个测试中 75 failed / 1019 passed / 1 expected fail / 5 skipped。
- 失败横跨 JS 边界相关语义：`shallowRef` 嵌套对象被标记为 reactive、`triggerRef` 在手动触发前已重跑 effect、hook effect/作用域未按预期清理，以及 owned mount、列表、Transition、RouterView 和多个实际页面交互回归。这些失败不能在本任务中通过调整预算或扩大生产修改规避，需回到引入回归的边界迁移任务修复后重跑。
- 因验证命令使用 `&&`，`pnpm exec vitest run --project unit scripts/__tests__/runtime-vapor-artifacts.spec.ts` 未执行；性能命令未执行，`post-boundary-current.json`、pre/post 表、Wasm 导出/hash 和交接清单均未生成。
- 按串行依赖未执行性能预算任务；当前工作树中亦不存在 `.plans/2026-08-25-性能架构优化/10-固化全链路性能预算.md`。
