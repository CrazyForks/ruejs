# 任务 10: 迁移锚点、范围与静态渲染

批次：【批次 10】 依赖批次 9

状态：未开始

目的：迁移 `renderAnchor`、`renderBetween`、静态节点和 replace/text patch 的范围所有权语义。

来源任务：无

预计会话范围：聚焦锚点与连续 DOM range，不处理组件实例和 owned mount。

## 文件

- 新建：`packages/runtime-vapor/js-runtime/render/anchor.js`
- 新建：`packages/runtime-vapor/js-runtime/render/range.js`
- 新建：`packages/runtime-vapor/js-runtime/render/static.js`
- 新建：`packages/runtime-vapor/js-runtime/render/helpers.js`
- 新建：`packages/runtime-vapor/js-runtime/patch/replace.js`
- 新建：`packages/runtime-vapor/js-runtime/patch/text.js`
- 修改：`packages/runtime-vapor/js-runtime/create-rue.js`
- 测试：`packages/runtime/__tests__/runtimeVapor.js-anchor-range-parity.spec.ts`
- 测试：`packages/runtime/__tests__/range.actual.spec.tsx`
- 测试：`packages/runtime/__tests__/renderAnchor.component-replacement.spec.tsx`

## 上下文

- 对照 Rust `render/anchor.rs`、`range.rs`、`range_ops.rs`、`static_mount.rs` 和 `render_patch/replace*.rs`。
- 不修改 `vapor-helpers.ts`、`vapor-helpers-vapor.ts` 或 compiler 的 compact/direct-root/compiled-row 快路径；这些只作为兼容性回归。

## 测试计划

- 行为：空/单/多节点范围、相邻锚点、静态内容、文本替换和跨组件 replacement 在两后端 DOM 结果与边界相同。
- 失败验证测试：新增 range 参数矩阵差分测试，要求 JS 后端执行上述操作。
- 失败验证命令：`pnpm exec vitest run --project unit-jsdom packages/runtime/__tests__/runtimeVapor.js-anchor-range-parity.spec.ts`
- 预期失败原因：JS Runtime 只有容器 mount，没有范围和 patch 实现。
- 通过验证命令：`pnpm exec vitest run --project unit-jsdom packages/runtime/__tests__/runtimeVapor.js-anchor-range-parity.spec.ts packages/runtime/__tests__/range.actual.spec.tsx packages/runtime/__tests__/renderAnchor.component-replacement.spec.tsx`
- 模拟策略：使用真实 jsdom 节点和 adapter；不模拟 DOM range。

## 步骤

1. 写空范围、嵌套范围、相邻锚点和替换顺序的失败差分测试。
2. 确认 Rust 参考行为稳定、JS 能力缺失。
3. 实现统一 range 表示、静态挂载和 replace/text patch。
4. 覆盖节点已移动、父级缺失和卸载后的显式错误。
5. 运行现有 range、anchor 和 compiled-row 兼容回归。

## 验证

- 运行：通过验证命令与 `pnpm exec vitest run --project unit-jsdom packages/runtime/__tests__/vaporKeyedList.fast-path.spec.ts`。
- 预期：DOM 序列和边界一致，列表快路径测试保持通过，受保护 helper 无修改。
- 所需证据：失败/通过输出、DOM 序列、range 边界表和受保护文件检查。

## 完成

只有所有公开范围渲染方法由 JS 等价承载，并且当前 compiled-row 路径没有退化时才算完成。
