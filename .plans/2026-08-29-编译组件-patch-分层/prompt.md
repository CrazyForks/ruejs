# 编译组件 patch 分层任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。开始前必须先完成 `.plans/2026-08-29-runtime-vapor-目录收敛/` 全部批次。

# 批次 1

## 任务 1: 定义组件更新模式协议【批次 1】依赖 runtime-vapor 目录收敛批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/1-定义组件更新模式协议.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认 `.plans/2026-08-29-runtime-vapor-目录收敛/` 全部批次已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 2

## 任务 2: 编译组件仅同步响应式 props【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/2-编译组件仅同步响应式-props.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认批次 1 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 3

## 任务 3: 规范化 h 兼容元素协议【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/3-规范化-h-兼容元素协议.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认批次 2 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 4

## 任务 4: 拆分核心与兼容挂载图【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/4-拆分核心与兼容挂载图.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认批次 3 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 5

## 任务 5: 固化体积与编译边界【批次 5】依赖批次 4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/5-固化体积与编译边界.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认批次 4 已完成。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 6

## 任务 6: 全量回归与交付【批次 6】依赖批次 5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-编译组件-patch-分层/6-全量回归与交付.md`。

开始前先阅读 `.plans/2026-08-29-编译组件-patch-分层/plan.md` 和该任务文件，确认批次 5 已完成。按任务文件中的验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-29-编译组件-patch-分层/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```
