# 任务 6: 全量回归与交付

批次：【批次 6】 依赖批次：批次 5

状态：未开始

目的：用全量构建、测试、体积和聚焦性能证明 patch 分层未牺牲组件、DOM 与兼容语义。

来源任务：无

预计会话范围：原则上只收集最终证据并修复可归因回归；不新增协议、入口或优化方向。

## 文件

- 新建证据：`temp/compiler-component-patch/final-summary.md`
- 新建证据：`temp/compiler-component-patch/final-size.json`
- 验证：`packages/runtime-vapor/src/**`
- 验证：`packages/runtime/__tests__/**`
- 验证：`packages/swc-plugin-rue/**`
- 验证：`scripts/__tests__/runtime-*.spec.ts`

## 上下文

- 硬边界：fine-grained props 更新不重跑组件；控制流和 h 仍正确重跑；compiled 图无 compat patch；full 图保留 h；生命周期、Context、Teleport、焦点、cleanup 和 keyed list 不回归。
- 工作区含独立的 runtime-vapor 目录收敛改动；失败必须先归因，禁止回退、覆盖或把生成 `dist/` 当手写源码修改。

## 验证计划

本任务不新增行为，因此不使用红绿 TDD。若发现回归，只修复能归因到任务 1–5 的协议、runtime、入口或测试文件，并重跑受影响批次及全量门禁。

## 步骤

1. 确认目录收敛计划已完成、任务 1–5 文件已删除，并区分本计划与其他工作树修改。
2. 运行 runtime-vapor check/build/test、Rust 全量、TypeScript check、unit tests 和工作区 build。
3. 重跑最终体积审计，确认 compiled-component 与 h-only 的模块来源和预算。
4. 汇总组件 render/setup/lifecycle 次数、DOM/focus/Teleport/Context/cleanup 证据。
5. 运行现有 js-framework 聚焦预算，确认组件协议分层没有影响 keyed list 和 clear 热路径。
6. 审查差异：编译核心无 head-record/stable-host/compat import，完整入口未删除 h，文档与真实行为一致。

## 验证

- 运行：`pnpm --filter @rue-js/runtime-vapor run check && pnpm --filter @rue-js/runtime-vapor run build && pnpm --filter @rue-js/runtime-vapor run test`
- 运行：`cargo test -p swc_plugin_rue`
- 运行：`pnpm run check`
- 运行：`pnpm run test-unit`
- 运行：`pnpm run build`
- 运行：`pnpm run size-runtime -- --output temp/compiler-component-patch/final-size.json`
- 运行：`pnpm run benchmark:js-framework -- --rounds 15 --warmup-rounds 3 --compare scripts/js-framework-performance-baseline.json --budget scripts/js-framework-performance-budget.json --output temp/compiler-component-patch/js-framework.json`
- 预期：所有命令退出码 0；体积/性能预算通过；编译与 h 两类更新语义均符合计划。
- 所需证据：Rust/TS/Vitest/build 测试数与退出码、体积/moduleIds 表、性能报告、render/setup/lifecycle/DOM 身份摘要、最终差异审查。

## 完成

只有全量门禁通过，且证据能明确说明“核心仍保留哪些动态 patch、compat 独占哪些递归 patch、编译组件减少了哪些执行”时才标记完成。无关环境失败可标记 `已完成，待回归`；可归因失败不得完成。
