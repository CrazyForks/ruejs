# JSX 最终展示值自动解包任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。本计划应在 `.plans/2026-09-06-useState完全兼容React/` 执行完成后开始，不要让两个计划并行写入。

# 批次 1

## 任务 1: 建立 Ref 展示值契约【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-JSX最终展示值自动解包/1-建立Ref展示值契约.md`。

开始前先阅读 `.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 和该任务文件，确认 `.plans/2026-09-06-useState完全兼容React/` 已执行完成、没有相关会话仍在写入且依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 2

## 任务 2: 接入客户端展示边界【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-JSX最终展示值自动解包/2-接入客户端展示边界.md`。

开始前先阅读 `.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 和该任务文件，确认依赖批次 1 已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 3: 统一服务端展示语义【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-JSX最终展示值自动解包/3-统一服务端展示语义.md`。

开始前先阅读 `.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 和该任务文件，确认依赖批次 1 已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 3

## 任务 4: 更新 Ref 展示文档【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-JSX最终展示值自动解包/4-更新Ref展示文档.md`。

开始前先阅读 `.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 和该任务文件，确认依赖批次 2 已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 4

## 任务 5: 执行最终回归【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-JSX最终展示值自动解包/5-执行最终回归.md`。

开始前先阅读 `.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 和该任务文件，确认依赖批次 3 已满足且没有并行会话仍在写入。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-06-JSX最终展示值自动解包/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```
