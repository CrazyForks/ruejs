# 任务 10: 全量回归与体积交付

批次：【批次 6】 依赖批次：批次 5

状态：未开始

目的：以源码、编译输出、真实 bundle、SSR/RSC、全量测试和发布检查证明 compiler-only 迁移完成。

来源任务：无

预计会话范围：原则上只运行验证和修复本迁移直接造成的遗漏；不新增功能或恢复兼容 API。测试驱动开发不适用，因为本任务是交付审计。

## 文件

- 修改（仅修复门禁遗漏）：`scripts/__tests__/runtime-architecture.spec.ts`
- 修改（仅修复门禁遗漏）：`scripts/__tests__/runtime-size-audit.spec.ts`
- 修改（仅修复门禁遗漏）：`scripts/runtime-size-budget.json`
- 审查：`packages/swc-plugin-rue`
- 审查：`packages/runtime`
- 审查：`packages/text`
- 审查：`packages/rue-rsc`
- 审查：`packages/router`
- 审查：`packages/i18n`
- 审查：`packages/rue-design`

## 上下文

- 工作树在计划开始前已有大量相关未提交改动；最终差异审查必须区分既有修改与本计划新增修改，不得恢复用户工作。
- 允许负向测试字符串描述 `h/jsx`，但任何可执行导出、调用、包名、alias、依赖、runtime 模块或文档承诺都视为失败。

## 验证计划

- 行为：所有受支持 TSX 均由 Rue 编译；客户端、SSR、RSC、组件/Context/动态节点行为通过；发布包图与 bundle 不包含通用工厂或 JSX runtime。
- 聚焦验证命令：`cargo test -p swc_plugin_rue && pnpm run check && pnpm run test-unit && pnpm run test:text-unit`
- 全量验证命令：`pnpm run test:all && pnpm run release-check`
- 体积验证命令：`pnpm run size-runtime`
- 静态审计命令：`rg -n '@rue-js/jsx-runtime|@rue-js/jsx-dev-runtime|jsxImportSource|export .*\bh\b|\bh\s*\(' packages app examples scripts docs --glob '!docs/search-index.json'`
- 预期：所有命令退出码 0；静态审计只允许测试中的负向字符串断言且逐项人工确认；bundle moduleIds 无旧包与通用工厂模块。

## 步骤

1. 运行 Rust、TypeScript、unit、Text 聚焦验证，按首个根因修复本迁移遗漏。
2. 运行 runtime architecture、tree-shaking 和 size audit，记录 compiled/vapor/server 三类 bundle 的 min/gzip/brotli 与 moduleIds。
3. 运行全量 `test:all`，确认测试数量、失败数和跳过数没有异常变化。
4. 运行 `release-check`，验证声明、exports、workspace 和发布文件清单。
5. 执行全仓静态审计和 `git diff --check`，逐项确认仅剩负向测试文本；检查未覆盖用户改动未被重置。

## 验证

- 运行：`cargo test -p swc_plugin_rue`
- 运行：`pnpm run check && pnpm run test:all`
- 运行：`pnpm run size-runtime`
- 运行：`pnpm run release-check`
- 运行：`git diff --check`
- 预期：全部退出码 0；无旧 API/包图；性能预算不放宽；编译、DOM、SSR、RSC 行为证据齐全。
- 所需证据：各命令退出码与测试汇总、bundle 体积/moduleIds、静态审计审查清单、最终差异检查。

## 完成

只有在所有门禁通过且无兼容 runtime 残留时才删除本任务文件并报告完成；若无关工作树问题阻止全量命令，记录具体失败和聚焦通过证据，状态改为`已完成，待回归`。
