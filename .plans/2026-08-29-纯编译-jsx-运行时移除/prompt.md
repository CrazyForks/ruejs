# 纯编译 JSX 运行时移除任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 封闭编译器 JSX 出口【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/1-封闭编译器-jsx-出口.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 2

## 任务 2: 收窄组件与动态挂载 ABI【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/2-收窄组件与动态挂载-abi.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 3

## 任务 3: 迁移 runtime 与 SSR 测试【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/3-迁移-runtime-与-ssr-测试.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 4: 迁移生态生产源码【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/4-迁移生态生产源码.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 6: 迁移应用示例与文档【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/6-迁移应用示例与文档.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 7: 新增服务端 JSX 编译目标【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/7-新增服务端-jsx-编译目标.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 4

## 任务 5: 迁移 rue-design 测试【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/5-迁移-rue-design-测试.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

## 任务 8: 迁移 Text/RSC 并移除 shim【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/8-迁移-text-rsc-移除-shim.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 5

## 任务 9: 删除 JSX 包与工具链引用【批次 5】依赖批次 4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/9-删除-jsx-包与工具链引用.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 6

## 任务 10: 全量回归与体积交付【批次 6】依赖批次 5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-纯编译-jsx-运行时移除/10-全量回归与体积交付.md`。

开始前先阅读 `.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的导致无法运行验证，可以解除`已阻塞`，修改为状态为 `已完成，待回归验证`。
注意，`.plans/2026-08-29-纯编译-jsx-运行时移除/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```
