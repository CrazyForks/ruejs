# 任务 5: 迁移生态基础包

批次：【批次 5】 依赖批次 4

状态：未开始

目的：将 `@rue-js/router`、`@rue-js/store` 和 `@rue-js/i18n` 转为 ESM-only 发布。

来源任务：无

预计会话范围：三个单入口生态包使用相同的发布模式和相同验证故合并处理。

## 文件

- 新建：`scripts/__tests__/ecosystem-esm-package-contract.spec.ts`
- 修改：`packages/router/package.json`
- 修改：`packages/router/index.js`
- 修改：`packages/store/package.json`
- 修改：`packages/store/index.js`
- 修改：`packages/i18n/package.json`
- 修改：`packages/i18n/index.js`
- 测试：`scripts/__tests__/ecosystem-esm-package-contract.spec.ts`

## 上下文

- Router 依赖 Rue/Runtime，Store/I18n 依赖 Rue；三包当前均为单入口 `esm-bundler,cjs` 发布。

## 测试计划

- 行为：三个生态包的根入口仅解析到 ESM，完整构建和 pack 无 CJS。
- 失败验证测试：`ecosystem-esm-package-contract.spec.ts` 的三包发布与代表性 API import 契约。
- 失败验证命令：`node scripts/build.js '^router$' '^store$' '^i18n$' && pnpm exec vitest run scripts/__tests__/ecosystem-esm-package-contract.spec.ts --project unit`
- 预期失败原因：三包仍有 CJS 门面、require 条件、CJS 格式和 pack 文件。
- 通过验证命令：`node scripts/build.js '^router$' '^store$' '^i18n$' && pnpm exec vitest run scripts/__tests__/ecosystem-esm-package-contract.spec.ts --project unit`
- 模拟策略：不使用 mock；使用真实包导入和 dry-run pack。

## 步骤

1. 新增三包失败契约。
2. 将三个门面与清单转为 ESM-only，从 `buildOptions` 删除 CJS。
3. 完整重建三包并运行契约测试。

## 验证

- 运行：`node scripts/build.js '^router$' '^store$' '^i18n$' && pnpm exec vitest run scripts/__tests__/ecosystem-esm-package-contract.spec.ts --project unit`
- 预期：构建和测试退出 0，三个 dist/pack 无 CJS，代表性 API 可从 ESM 导入。
- 所需证据：修改前失败、通过数、三个 pack 列表和 API import 结果。

## 完成

仅当 Router、Store 和 I18n 的清单、入口、构建与 pack 均无 CJS 时才能标记完成。
