# Rue.js 全工作区仅 ESM 发布任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 统一现代版本基线【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/1-统一-现代版本-基线.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 2

## 任务 2: 迁移基础运行时包【批次 2】 依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/2-迁移-基础运行时-包.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 3

## 任务 3: 迁移服务端与 Rue 门面【批次 3】 依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/3-迁移-服务端-rue-门面.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 4

## 任务 4: 迁移 JSX 运行时【批次 4】 依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/4-迁移-jsx-运行时.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 5

## 任务 5: 迁移生态基础包【批次 5】 依赖批次 4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/5-迁移-生态基础包.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 6

## 任务 6: 迁移 Design 根与子路径【批次 6】 依赖批次 5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/6-迁移-design-根与子路径.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 7

## 任务 7: 收敛工具与已 ESM 包【批次 7】 依赖批次 6

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/7-收敛-工具与已-esm-包.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 8

## 任务 8: 删除内部 CJS 构建流程【批次 8】 依赖批次 7

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/8-删除-内部-cjs-构建流程.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```

# 批次 9

## 任务 9: 执行全工作区发布回归【批次 9】 依赖批次 1,2,3,4,5,6,7,8

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-29-rue-仅-esm-发布/9-执行-全工作区-发布回归.md`。

开始前先阅读 `.plans/2026-08-29-rue-仅-esm-发布/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因无关本次修改的问题无法运行验证，将状态更新为 `已完成，待回归`。
注意，`.plans/2026-08-29-rue-仅-esm-发布/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文的大小，还会干扰其他任务的执行。
```
