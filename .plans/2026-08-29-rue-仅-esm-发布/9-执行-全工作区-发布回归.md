# 任务 9: 执行全工作区发布回归

批次：【批次 9】 依赖批次 1,2,3,4,5,6,7,8

状态：未开始

目的：验证全部 `@rue-js/*` 包的清单、导出、构建、npm pack、类型和相关测试在 ESM-only 后统一通过。

来源任务：无

预计会话范围：仅新增全局发布门禁并执行回归，不引入新功能或超出前八个任务的重构。

## 文件

- 新建：`scripts/__tests__/workspace-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/workspace-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/core-runtime-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/rue-server-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/jsx-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/ecosystem-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/tooling-esm-package-contract.spec.ts`
- 测试：`scripts/__tests__/esm-only-package-builder.spec.ts`
- 测试：`scripts/__tests__/rue-design-package-contract.spec.ts`

## 上下文

- 全局门禁应自动发现 16 个 `@rue-js/*` 包，对 JS 包、Wasm 包和私有包分类验证，不把第三方 CJS 互操作测试判为发布回归。

## 测试计划

- 行为：所有公开 JS 包均为 type module、无 require/CJS 导出、无 CJS buildOptions/pack 文件；全局构建和单元测试通过。
- 失败验证测试：`workspace-esm-package-contract.spec.ts` 的16 包发现、分类、清单、导出、dist 和 dry-run pack 门禁。
- 失败验证命令：`pnpm exec vitest run scripts/__tests__/workspace-esm-package-contract.spec.ts --project unit`
- 预期失败原因：在聚合门禁刚写入时，任何前置任务遗漏的 CJS 条件、格式或 pack 文件都会使其失败。
- 通过验证命令：`pnpm run build && pnpm --filter @rue-js/rsc run build && pnpm --dir packages/text build && pnpm exec vitest run scripts/__tests__/workspace-esm-package-contract.spec.ts --project unit && pnpm check && pnpm run test-unit && pnpm run test:text-unit && pnpm --filter @rue-js/rsc run test-unit`
- 模拟策略：不使用 mock；使用真实全局构建、Node 解析、npm dry-run pack 和工作区测试。

## 步骤

1. 新增自动发现所有 `@rue-js/*` 包的全局 ESM 门禁，先确认它能抓住遗漏。
2. 运行不带格式过滤的全工作区构建，清除忽略 dist 中的历史 CJS 文件。
3. 运行全局门禁、TypeScript、核心单元、Text 单元和 RSC 转换测试。
4. 搜索所有包 dist 的 CJS 文件，运行 `git diff --check`，审查差异只包含本计划范围。

## 验证

- 运行：`pnpm run build && pnpm --filter @rue-js/rsc run build && pnpm --dir packages/text build && pnpm exec vitest run scripts/__tests__/workspace-esm-package-contract.spec.ts --project unit && pnpm check && pnpm run test-unit && pnpm run test:text-unit && pnpm --filter @rue-js/rsc run test-unit && git diff --check`
- 运行：`find packages -type f -path '*/dist/*' -name '*cjs*' -print`
- 预期：联合门禁退出 0，16 个包被分类验证，dist CJS 搜索无输出。
- 所需证据：全局构建退出码、各测试项目的通过/失败数、16 包 pack 结果、空的 CJS 搜索和差异检查。

## 完成

本任务只添加发布门禁。只有在 16 个包都有当前工作树的 ESM-only 证据，且全部联合回归通过时才能标记完成。
