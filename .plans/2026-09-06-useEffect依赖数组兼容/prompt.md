# useEffect 依赖数组兼容任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

线性顺序：批次 1 的任务 1、2 可并行；二者均完成后执行批次 2，任务 3、4 可并行。

# 批次 1

## 任务 1: 实现编译运行时依赖语义【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useEffect依赖数组兼容/1-实现编译运行时依赖语义.md`。

开始前先阅读 `.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 和该任务文件，确认依赖已满足。当前为批次 1，无依赖批次，可与任务 2 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以解除 `已阻塞`，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

## 任务 2: 编译依赖为惰性读取器【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useEffect依赖数组兼容/2-编译依赖为惰性读取器.md`。

开始前先阅读 `.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 和该任务文件，确认依赖已满足。当前为批次 1，无依赖批次，可与任务 1 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以解除 `已阻塞`，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

# 批次 2

## 任务 3: 验证真实编译组件行为【批次 2】 依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useEffect依赖数组兼容/3-验证真实编译组件行为.md`。

开始前先阅读 `.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 和该任务文件，确认批次 1 的任务 1、2 均已满足。当前为批次 2，可与任务 4 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以解除 `已阻塞`，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```

## 任务 4: 记录三种依赖模式【批次 2】 依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useEffect依赖数组兼容/4-记录三种依赖模式.md`。

开始前先阅读 `.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 和该任务文件，确认批次 1 的任务 1、2 均已满足。当前为批次 2，可与任务 3 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以解除 `已阻塞`，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useEffect依赖数组兼容/plan.md` 为只读，不要修改该文件的任何部分，这会增加上下文大小，还会干扰其他任务执行。
```
