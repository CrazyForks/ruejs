# 任务 5: 全量回归与性能交付

批次：【批次 4】 依赖批次：批次 3

状态：未开始

目的：以全量测试、消费端体积和聚焦 render/DOM 计数证明瘦身未牺牲语义，并形成可审查交付证据。

来源任务：无

预计会话范围：原则上只做回归、证据收集和可归因修复；不新增 lowering 形状、API 或运行时抽象。

## 文件

- 新建证据：`temp/compiler-runtime-boundary/final-summary.md`
- 新建证据：`temp/compiler-runtime-boundary/final-size.json`
- 验证：`packages/swc-plugin-rue/**`
- 验证：`packages/runtime/__tests__/**`
- 验证：`scripts/__tests__/runtime-*.spec.ts`

## 上下文

- 工作树已有用户的 compiled keyed-list/clear 性能修改；回归失败必须先按差异和测试归因，不能覆盖或回退无关改动。
- 硬边界：安全 compiled 输出无 `vapor()`/默认 JSX runtime；必要 fallback 保留；普通 fallback 不多执行；控制流、Context、生命周期和焦点语义通过。

## 验证计划

本任务不新增行为，因此不使用红绿 TDD。若发现回归，只在能归因到任务 2–4 的文件和已定义行为内修复，并重新运行受影响批次与全量门禁。

## 步骤

1. 确认任务 1–4 文件已删除，检查工作树并区分本计划与用户已有修改。
2. 运行 Rust 全量测试、SWC Wasm 构建、TypeScript 检查、单元测试和工作区构建。
3. 运行最终体积审计，比较基线与最终 JSON，列出 compiled、Vapor、`h-only`、自动 JSX runtime 的来源和体积变化。
4. 汇总安全正例/不安全 fallback 的生成代码，及普通/控制流组件 render 次数、DOM 身份、焦点和生命周期证据。
5. 运行现有 js-framework 聚焦性能预算；若本计划使总体或 clear 门禁回退，只修复可归因问题并整轮重跑。
6. 审查最终差异，确认未删除 `h()`、未把必要 Vapor 边界降级、未放宽既有预算。

## 验证

- 运行：`cargo test -p swc_plugin_rue`
- 运行：`pnpm --filter @rue-js/swc-plugin-rue run build`
- 运行：`pnpm run check`
- 运行：`pnpm run test-unit`
- 运行：`pnpm run build`
- 运行：`pnpm run size-runtime -- --output temp/compiler-runtime-boundary/final-size.json`
- 运行：`pnpm run benchmark:js-framework -- --rounds 15 --warmup-rounds 3 --compare scripts/js-framework-performance-baseline.json --budget scripts/js-framework-performance-budget.json --output temp/compiler-runtime-boundary/js-framework.json`
- 预期：所有命令退出码 0；体积预算无失败；性能报告通过既有门禁；正反例和 render 次数符合计划架构边界。
- 所需证据：Rust/TS/Vitest/build 测试数与退出码、最终体积表及模块来源、性能报告、生成代码正反例、render/DOM/生命周期断言摘要、差异审查结果。

## 完成

只有全量门禁通过且最终证据说明“哪些场景不再需要 Vapor、哪些仍需要、`h()` 成本是否隔离”时才标记完成。无关环境失败可标记 `已完成，待回归`；可归因语义或预算失败必须保持未完成或记录为阻塞。
