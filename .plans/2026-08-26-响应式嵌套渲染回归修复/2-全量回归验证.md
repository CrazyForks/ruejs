# 任务 2: 全量回归验证

批次：【批次 2】 依赖批次 1

状态：已完成

目的：确认修复覆盖用户报告的三处失败，并未破坏 TypeScript 检查、兼容约束或其余测试。

来源任务：无

预计会话范围：只执行仓库级验证和审查最终差异，不新增功能或顺手修复无关失败。

## 文件

- 修改：无
- 测试：`packages/runtime-vapor/tests/test_nested_effect.rs`
- 测试：`packages/rue-design/src/components/input/__tests__/Input.spec.tsx`
- 测试：`packages/rue-design/src/components/transfer/__tests__/Transfer.spec.tsx`

## 上下文

- 全量入口 `npm run test` 会重建 runtime/rue/rue-design 测试产物并运行兼容检查与 Vitest。
- 工作区原有未提交修改必须保留，最终仅报告本次实际触及内容。

## 测试计划

- 行为：从干净重建的测试产物运行时，原三处失败消失且没有新增失败。
- 失败验证测试：复用任务 1 已记录的三个失败基线，不新增重复测试。
- 失败验证命令：`pnpm exec vitest run packages/rue-design/src/components/input/__tests__/Input.spec.tsx packages/rue-design/src/components/transfer/__tests__/Transfer.spec.tsx --project rue-design-jsdom`
- 预期失败原因：修复前稳定出现 3 个失败。
- 通过验证命令：`npm run test`
- 模拟策略：使用仓库原生测试环境。

## 步骤

1. 确认任务 1 的聚焦测试已通过。
2. 运行 `npm run test`，等待构建、兼容检查和全部 Vitest 完成。
3. 若出现新失败，仅处理与任务 1 修改存在直接因果关系的问题。
4. 运行 `git diff --check` 并审查最终 diff。

## 验证

- 运行：`npm run test`
- 运行：`git diff --check`
- 预期：全量测试退出码为 0，格式检查无错误。
- 所需证据：测试文件/用例通过总数、跳过和预期失败数、两个命令退出码。

## 完成

- `npm run test` 退出码为 0；Vitest 记录 391 个测试文件全部通过，1910 个测试通过、1 个预期失败、6 个跳过。
- `pnpm --filter @rue-js/runtime-vapor test` 通过，新调度回归用例在 Node/Wasm 核心测试集中实际执行。
- `git diff --check` 通过，无调试日志残留；未修改现有失败用例断言或增加跳过。
