# useState 完全兼容 React 任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

线性顺序：批次 1 的任务 1、2、3 可并行；全部完成后并行执行批次 2 的任务 4、5；随后并行执行批次 3 的任务 6、7、8、9；最后执行批次 4 的任务 10。

# 批次 1

## 任务 1: 实现编译状态核心契约【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/1-实现编译状态核心契约.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认依赖已满足。当前为批次 1，无依赖批次，可与任务 2、3 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 2: 编译状态值读取【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/2-编译状态值读取.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认依赖已满足。当前为批次 1，无依赖批次，可与任务 1、3 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 3: 统一普通 Hook 与公开类型【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/3-统一普通Hook与公开类型.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认依赖已满足。当前为批次 1，无依赖批次，可与任务 1、2 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 2

## 任务 4: 简化 Text 状态适配【批次 2】 依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/4-简化Text状态适配.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 1 已完成。当前为批次 2，可与任务 5 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 5: 验证真实编译与自定义 Hook【批次 2】 依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/5-验证真实编译与自定义Hook.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 1 已完成。当前为批次 2，可与任务 4 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 3

## 任务 6: 迁移应用与示例【批次 3】 依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/6-迁移应用与示例.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 2 已完成。当前为批次 3，可与任务 7、8、9 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 7: 迁移设计组件【批次 3】 依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/7-迁移设计组件.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 2 已完成。当前为批次 3，可与任务 6、8、9 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 8: 迁移现有测试与编译入口【批次 3】 依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/8-迁移现有测试与编译入口.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 2 已完成。当前为批次 3，可与任务 6、7、9 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

## 任务 9: 更新 useState 文档【批次 3】 依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/9-更新useState文档.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 2 已完成。当前为批次 3，可与任务 6、7、8 并行。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 4

## 任务 10: 执行最终回归【批次 4】 依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-06-useState完全兼容React/10-执行最终回归.md`。

开始前先阅读 `.plans/2026-09-06-useState完全兼容React/plan.md` 和该任务文件，确认批次 3 已完成。当前为批次 4，不与其他任务并行。按任务文件中的验证计划执行；发现本计划相关缺陷时先补失败测试再修复，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，将状态修改为 `已完成，待回归`。
注意，`.plans/2026-09-06-useState完全兼容React/plan.md` 为只读，不要修改该文件的任何部分。
```
