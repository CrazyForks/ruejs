# 任务 18: 全量回归与性能交接

批次：【批次 18】 依赖批次 17

状态：已阻塞

目的：证明 TS 恢复与底层修复通过完整验证，并恢复原边界计划任务 17 的性能预算交接。

来源任务：`.plans/2026-08-25-runtime-vapor-js-边界重构/17-全量回归性能护栏与预算交接.md`

预计会话范围：只做全量验证、产物/类型/注释审计、性能测量和交接；若发现新行为失败，回到对应来源任务修复。

## 文件

- 验证：`packages/runtime-vapor/`
- 验证：`packages/runtime/`
- 验证：`packages/swc-plugin-rue/`
- 验证：`scripts/__tests__/runtime-vapor-typescript.spec.ts`
- 验证：`scripts/__tests__/runtime-vapor-artifacts.spec.ts`
- 证据：`temp/runtime-vapor-js-boundary/post-boundary-current.json`
- 交接：`.plans/2026-08-25-runtime-vapor-js-边界重构/17-全量回归性能护栏与预算交接.md`
- 后续：`.plans/2026-08-25-性能架构优化/10-固化全链路性能预算.md`

## 上下文

- 不修改旧边界计划 `plan.md`；性能 compiler/DOM/list/runner/baseline/budget 均为受保护成果。
- 当前工作树中性能任务 10 文件曾确认缺失；若执行时仍缺失，只报告该外部交接阻塞，不自行重建预算任务或修改预算。
- 只有所有代码/类型/注释/产物/性能证据齐全后，才能删除本任务与旧任务 17。

## 验证计划

- 行为：40/40 TS、零手写 JS、中文注释 100%、完整构建/类型/Rust/unit/artifact/benchmark 全部满足旧任务 17 护栏。
- 验证命令：`pnpm --filter @rue-js/runtime-vapor run check-ts && node packages/runtime-vapor/scripts/check-legacy-rust-comments.mjs --verify-ts && pnpm run check && cargo test --manifest-path packages/runtime-vapor/Cargo.toml && cargo test --manifest-path packages/swc-plugin-rue/Cargo.toml --test js_framework_benchmark && pnpm run test-unit && pnpm exec vitest run --project unit scripts/__tests__/runtime-vapor-typescript.spec.ts scripts/__tests__/runtime-vapor-artifacts.spec.ts`
- 性能命令：`pnpm run benchmark:js-framework -- --output temp/runtime-vapor-js-boundary/post-boundary-current.json`
- 预期失败原因：不适用；本任务不修改行为，失败必须定位回任务 3-17，不能调预算掩盖。
- 模拟策略：全部使用真实 tsc、Wasm、Vitest、artifact bundle 和 benchmark。

## 步骤

1. 审查 diff，确认 `Teleport.ts`、性能预算/基线和旧 plan.md 无改动。
2. 执行 TS/注释/source guard、workspace check、Rust、compiler、全量 unit 和 artifact 验证。
3. 运行同机 benchmark，按旧任务 17 的 pre/post 规则生成对比表，不修改阈值。
4. 若失败可归因本计划，回到来源任务修复后重跑；无关环境失败按任务状态规则记录。
5. 证据齐全后解除旧任务 17 的阻塞、完成其交接并按其规则删除；若性能任务 10 存在再按原串行要求执行，不存在则明确报告缺失。

## 验证

- 运行：上述验证命令、性能命令、`git diff --exit-code -- packages/runtime/src/components/Teleport.ts scripts/runtime-size-baseline.json scripts/js-framework-performance-baseline.json .plans/2026-08-25-runtime-vapor-js-边界重构/plan.md`。
- 预期：所有命令退出 0；unit 零失败；单 browser Wasm；无 >5% 可复现退化；受保护文件零 diff。
- 所需证据：40/40 TS、注释覆盖、所有退出码/测试数、Wasm export/hash、post JSON、pre/post 表、受保护 diff、性能任务 10 存在性检查。

## 完成

本任务不适用 TDD。完成时删除本任务文件和旧边界任务 17；若任何证据不足，只按各自任务文件规则记录阻塞，不宣布计划完成。

## 阻塞记录（2026-08-26，修复后复核）

- 依赖确认：批次 17 的行为回归阻塞已定位并修复；本次只处理 TS 恢复、底层 effect/DOM 语义和回归交接，未修改性能预算、baseline 或 `packages/runtime/src/components/Teleport.ts`。
- 阻塞修复：补齐 standalone runtime-vapor 全局声明；修复嵌套 effect 激活祖先重入，并使用当前 effect 快路径保护调度性能；修复 KeepAlive 边界压缩和 checkbox/Swap 的真实 DOM 回归。新增嵌套 effect 回归用例。
- 差异审计：`git diff --check`、`cargo fmt --manifest-path packages/runtime-vapor/Cargo.toml --check` 均退出 0；对 `Teleport.ts`、两份性能 baseline、旧边界 `plan.md` 和本 TS 恢复 `plan.md` 执行受保护 diff 检查退出 0。
- TS/注释/类型：`check-ts` 退出 0，`40 targets, 40 migrated`；历史中文注释 `578/578`，SHA-256 `1d8481655a75c83eebebc79f2174bb0e544462f5bdd981ed11ecfe336707e03e`，missing/extra/no target/invalid 均为 0；`pnpm run check` 退出 0。
- Rust/compiler：`cargo test --manifest-path packages/runtime-vapor/Cargo.toml` 退出 0，11 passed / 0 failed / 1 ignored；`cargo test --manifest-path packages/swc-plugin-rue/Cargo.toml --test js_framework_benchmark` 退出 0，4 passed / 0 failed。
- unit/artifact：修复后曾完成全量 unit，297 files passed，1113 passed / 1 expected fail / 5 skipped。最终 effect 快路径修正后重跑完整命令时，整机事件循环严重饥饿，Vitest 于 3937.83s 后以 283 files passed / 7 failed、1037 passed / 4 failed / 1 expected fail / 5 skipped 退出 1；失败由 SWC 60s transform timeout、worker response timeout 和 10s 用例被拖延到 10–17 分钟引起。未改配置，将同一批 7 个失败文件限制为单 worker 复跑，7 files / 25 tests 全过，耗时 8.05s，确认为无关本次修改的环境并发资源失败。artifact/TypeScript 契约单独复跑 2 files / 19 tests 全过。
- Wasm 产物：`pkg-vapor` 只有一个 browser Wasm；SHA-256 `175892ae82a9a2738efc022ce7211b0bb0169b5c054f0a0e3e8545f53595a89a`；导出已实测包含 `createEffect`、`createSignal`、`watch`、`batch`、effect scope/debug owner 等 canonical API。
- benchmark：按指定命令生成 `temp/runtime-vapor-js-boundary/post-boundary-current.json`，Chrome `148.0.7778.97`，Rue/rue-signal/Vue 每个 CPU、DOM、heap、first-paint 项均有 5 个有效样本，artifactCount 6，hashesStable true。
- pre/post：Rue CPU 中位数总和 `143.3→155.4ms`（+8.44%），rue-signal `102.4→110.7ms`（+8.11%），Vue 对照 `73.2→76.7ms`（+4.78%）；按 Vue 对照归一化后 Rue 为 +3.50%、rue-signal 为 +3.17%。Rue/rue-signal 三个 heap 场景为 +0.85%～+1.18%；first-paint 为 Rue `40→44ms`、signal `44→44ms`、Vue `36→44ms`。DOM mutation 契约不变。
- size 硬阻塞：JS raw `124027→175126` bytes（+41.20%）、Brotli `28948→42180` bytes（+45.71%）；Wasm raw `165771→134218` bytes（-19.03%）、Brotli `54470→46374` bytes（-14.86%）；合并 raw `289798→309344` bytes（+6.74%）、Brotli `83418→88554` bytes（+6.16%）。该确定性产物增幅超过 5% 护栏，且 Hook-heavy 至少 10% 与综合至少 5% 改善目标没有实测达成；不允许通过修改预算或 baseline 规避，因此任务仍为“已阻塞”，不删除本任务或旧任务 17。
- 外部交接：`.plans/2026-08-25-性能架构优化/10-固化全链路性能预算.md` 仍不存在；未重建该任务，也未修改任何性能预算。
