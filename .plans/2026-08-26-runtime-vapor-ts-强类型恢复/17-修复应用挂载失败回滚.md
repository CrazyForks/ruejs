# 任务 17: 修复应用挂载失败回滚

批次：【批次 17】 依赖批次 16

状态：已完成，待回归

目的：恢复根组件 mount 抛错后的容器所有权、缓存错误和后续 mount 拒绝语义。

来源任务：原边界计划任务 17 的剩余失败

预计会话范围：只处理 app/runtime entry 的 mount 事务与错误状态，不修改 useApp 测试契约或上层应用组件。

## 文件

- 可能修改：`packages/runtime-vapor/js-runtime/app.ts`
- 可能修改：`packages/runtime-vapor/js-runtime/create-rue.ts`
- 可能修改：`packages/runtime-vapor/runtime-entry-wrap.ts`
- 可能修改：`packages/runtime-vapor/js-runtime/render/container.ts`
- 测试：`packages/runtime/__tests__/useApp.mount-ownership.spec.tsx`

## 上下文

- 2026-08-26 已复现：5 tests 中 1 failed；首次 root throw 正常，随后另一个 app 对同一 host 的 mount 未重抛缓存的 `root mount failed`。
- 现有契约要求失败 mount 回滚 DOM，但保留容器的失败所有权/错误状态，使竞争 app 不能接管；`renderCount` 保持 1。
- 错误状态必须在 runtime/app/controller 类型中显式建模，不能依赖未声明动态私有键。

## 测试计划

- 行为：root mount 抛错后 DOM 为空、原 root 不重渲染、同容器的后续 app mount 重抛原错误。
- 失败验证测试：扩充现有测试以同时断言 owner、pending error 和清理状态，不修改既有期望。
- 失败验证命令：`pnpm exec vitest run --project unit-jsdom packages/runtime/__tests__/useApp.mount-ownership.spec.tsx`
- 预期失败原因：当前第二次 `recoveredApp.mount(host)` 不抛错，现状为 1 failed / 4 passed。
- 通过验证命令：`pnpm --filter @rue-js/runtime-vapor run check-ts && pnpm exec vitest run --project unit-jsdom packages/runtime/__tests__/useApp.mount-ownership.spec.tsx packages/runtime/__tests__/runtimeVapor.current-boundary-contract.spec.ts packages/runtime/__tests__/runtimeVapor.js-container-parity.spec.ts packages/runtime/__tests__/runtimeVapor.js-error-keepalive-parity.spec.ts`
- 模拟策略：使用真实 useApp/runtime/jsdom；不模拟 mount 或错误桥。

## 步骤

1. 复现现有 1/5 失败并记录首次/二次 mount 的状态流。
2. 增加可观察的底层事务断言，区分 DOM 回滚和容器失败所有权。
3. 在最窄 app/entry/container TS 模块保存并重放原错误，保持成功 mount 行为。
4. 运行聚焦及边界/error/container 周边测试。
5. 删除临时诊断并审查错误状态不会跨容器泄漏。

## 验证

- 运行：上述通过命令。
- 预期：退出码 0；mount ownership 5/5 通过；相关 parity 零失败；renderCount 仍为 1。
- 所需证据：原始 1 failed / 4 passed、状态流根因、新断言、最终通过数、跨容器隔离结果。

## 完成

失败 mount 的 DOM 与错误/所有权事务语义同时恢复，不通过更改测试或上层 useApp 绕过。

## 执行记录（2026-08-26）

- 红灯证据：原始聚焦命令稳定复现 `1 failed / 4 passed`；竞争 app 的第二次 `mount(host)` 未抛错。
- 根因：根组件同步抛错时，外层 `useApp` 会清理 DOM 并回滚其共享 reservation，但底层 app controller 没有显式挂载事务，导致失败容器 owner 与原始错误一起丢失。
- 修复：在 runtime state/app controller 中显式建模 `mounting | mounted | failed` 事务；仅在根组件函数自身抛错时缓存原始错误，失败后的 unmount 只清理 DOM、不释放失败事务；成功 unmount/free 正常释放事务。
- 新断言：原 owner 重试与竞争 app 均重放同一个原始 `Error`；`renderCount === 1`；失败 host 为空且没有 `data-rue-app`；另一 host 可独立成功挂载。
- 通过：`pnpm --filter @rue-js/runtime-vapor run check-ts`，`40 targets, 40 migrated`。
- 通过：ownership、Vapor setup 回滚、container parity、error/keepalive parity 联合验证，`4 files / 18 tests` 全部通过；其中 ownership `5/5`、Vapor setup 回滚 `8/8`，证明错误不跨容器泄漏且未破坏非根函数错误的原接管语义。
- 待回归原因：任务文件指定的完整命令为 `1 failed / 3 passed` test files、`1 failed / 14 passed` tests；唯一失败是前序批次的 `vapor` entry 多导出 `__rueActivateEffectOwnerTracking`、`__rueActivateRenderTriggered`。相关 `vapor.ts` / `vapor.node.ts` 修改时间为本任务开始前，且不在本任务改动范围；本次曾引入的 runtime 方法表差异已收回，对应边界断言已恢复通过。
- 未修改：`plan.md`、`packages/runtime/src/components/Teleport.ts`、性能预算与基线。
