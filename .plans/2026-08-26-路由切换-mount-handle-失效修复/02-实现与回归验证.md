# 任务 02：实现与回归验证

状态：已完成

## 步骤

- 在正确的句柄生产/重放边界修复重复使用。
- 保留 default mount handle 的单次消费与 stale 校验。
- 执行路由、runtime-vapor 聚焦测试和类型检查。
- 执行全量 `npm run test`。

## 完成标准

- 新增回归测试通过。
- 现有路由与 mount input 协议测试通过。
- 类型检查与全量测试通过。

## 完成结果

- `normalizeMountInput` 只对显式 repeatable 的已消费结果执行一次工厂重放；普通 stale/unknown handle 仍走原协议错误。
- 路由回访、组件结果重放、mount input 协议以及 Input/Transfer/复杂列表聚焦回归均通过。
- `pnpm run check` 通过；`npm run test`：391 个测试文件全部通过，1910 个测试通过、1 个预期失败、6 个跳过。
