# 原始请求

切换路由时报错：

`Rue runtime: render input not supported on the default path: stale or unknown mount handle 14`

要求直接修复底层问题，不通过修改测试、关闭校验或允许重复消费 stale handle 绕过。

# 批次 3

## 任务 03：绑定组件渲染 runtime【批次 3】依赖批次 1、2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-26-路由切换-mount-handle-失效修复/03-绑定组件渲染runtime.md`。

开始前先阅读 `.plans/2026-08-26-路由切换-mount-handle-失效修复/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-26-路由切换-mount-handle-失效修复/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 4

## 任务 04：跨侧边栏异步子路由恢复【批次 4】依赖批次 1、2、3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-08-26-路由切换-mount-handle-失效修复/04-跨侧边栏异步子路由恢复.md`。

开始前先阅读 `.plans/2026-08-26-路由切换-mount-handle-失效修复/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，只处理该任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因导致无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-08-26-路由切换-mount-handle-失效修复/plan.md` 为只读，不要修改该文件的任何部分。
```
